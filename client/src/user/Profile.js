import React from "react";
import Base from "../core/Base";
const Profile = () => {
  return (
    <Base title="profile page">
      <h1>this is propfile page</h1>
    </Base>
  );
};

export default Profile;
